"""(In progress) Use this to call other scripts to run out the rollouts.

Done because the other way of doing it can be susceptible to changes in the
configuration file. Ideally we have a nice way of loading in a configuration
file and running it.
"""


if __name__ == "__main__":
    pass
